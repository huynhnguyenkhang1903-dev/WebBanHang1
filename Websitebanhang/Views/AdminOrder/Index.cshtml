@model IEnumerable<Websitebanhang.Models.Order>

@{
    ViewData["Title"] = "Qu?n lý ??n hàng";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold"><i class="fa fa-shopping-cart text-success me-2"></i> Qu?n Lý ??n Hàng</h2>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Mã ?H</th>
                            <th>Khách hàng</th>
                            <th>Email</th>
                            <th>S? ?i?n tho?i</th>
                            <th>Ngày ??t</th>
                            <th>Ph??ng th?c</th>
                            <th>T?ng ti?n</th>
                            <th>Tr?ng thái</th>
                            <th>Hành ??ng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-bold">#@item.Id</td>
                                <td>@item.CustomerName</td>
                                <td>@item.Email</td>
                                <td>@item.Phone</td>
                                <td>@item.OrderDate.ToString("dd/MM/yyyy HH:mm")</td>
                                <td><span class="badge bg-secondary">@item.PaymentMethod</span></td>
                                <td class="text-danger fw-bold">@item.TotalAmount.ToString("N0") ?</td>
                                <td>
                                    <span class="badge @(item.Status == "Pending" ? "bg-warning text-dark" : item.Status == "Cancelled" ? "bg-danger" : "bg-success")">
                                        @item.Status
                                    </span>
                                </td>
                                <td>
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <div class="d-flex gap-1">
                                            @* Confirm *@
                                            @if (item.Status == "Pending")
                                            {
                                                <form asp-controller="AdminOrder" asp-action="Confirm" method="post" class="m-0 p-0">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@item.Id" />
                                                    <button type="submit" class="btn btn-sm btn-success">Duy?t</button>
                                                </form>
                                            }

                                            @* Shipping *@
                                            @if (item.Status == "Confirmed")
                                            {
                                                <form asp-controller="AdminOrder" asp-action="Shipping" method="post" class="m-0 p-0">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@item.Id" />
                                                    <button type="submit" class="btn btn-sm btn-primary">Giao</button>
                                                </form>
                                            }

                                            @* Delivered *@
                                            @if (item.Status == "Shipping")
                                            {
                                                <form asp-controller="AdminOrder" asp-action="Delivered" method="post" class="m-0 p-0">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@item.Id" />
                                                    <button type="submit" class="btn btn-sm btn-dark">Hoàn thành</button>
                                                </form>
                                            }

                                            @* Cancel (allow admin to cancel unless already delivered) *@
                                            @if (item.Status != "Delivered" && item.Status != "Cancelled")
                                            {
                                                <form asp-controller="AdminOrder" asp-action="Cancel" method="post" class="m-0 p-0" onsubmit="return confirm('B?n có ch?c mu?n h?y ??n này?');">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@item.Id" />
                                                    <button type="submit" class="btn btn-sm btn-danger">H?y</button>
                                                </form>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <a asp-controller="Account" asp-action="OrderDetails" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Xem</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
