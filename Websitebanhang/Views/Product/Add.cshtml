@model Websitebanhang.Models.Product

@{
    ViewData["Title"] = "Thêm Sản Phẩm Mới";
    Layout = "_AdminLayout";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2 class="fw-bold mb-4"><i class="fa fa-plus-circle text-success me-2"></i> Thêm Sản Phẩm Mới</h2>
            
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <form asp-action="Add" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Tên sản phẩm</label>
                            <input asp-for="Name" class="form-control" placeholder="Nhập tên sản phẩm..." />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-3 mb-3">
                                <label asp-for="Price" class="form-label fw-bold">Giá (VNĐ)</label>
                                <input asp-for="Price" type="number" class="form-control" />
                                <span asp-validation-for="Price" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="Stock" class="form-label fw-bold">Tồn kho</label>
                                <input asp-for="Stock" type="number" class="form-control" value="0" min="0" />
                                <span asp-validation-for="Stock" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="ExpiryDate" class="form-label fw-bold">Hạn sử dụng</label>
                                <input asp-for="ExpiryDate" type="date" class="form-control" />
                                <span asp-validation-for="ExpiryDate" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="CategoryId" class="form-label fw-bold">Danh mục</label>
                                <select asp-for="CategoryId" class="form-select" asp-items="@(new SelectList(ViewBag.Categories, "Id", "Name"))">
                                    <option value="">-- Chọn danh mục --</option>
                                </select>
                                <span asp-validation-for="CategoryId" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="SupplierId" class="form-label fw-bold">Nhà cung cấp</label>
                                <select asp-for="SupplierId" class="form-select" asp-items="@(new SelectList(ViewBag.Suppliers, "Id", "Name"))">
                                    <option value="">-- Không có --</option>
                                </select>
                                <span asp-validation-for="SupplierId" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="VoucherId" class="form-label fw-bold text-danger">Khuyến mãi (Voucher)</label>
                                <select asp-for="VoucherId" class="form-select border-danger" asp-items="@(new SelectList(ViewBag.Vouchers, "Id", "Code"))">
                                    <option value="">-- Không có khuyến mãi --</option>
                                </select>
                                <span asp-validation-for="VoucherId" class="text-danger"></span>
                            </div>

                            <div class="col-md-3 mb-3">
                                <label asp-for="Unit" class="form-label fw-bold text-primary">Đơn vị tính</label>
                                <select asp-for="Unit" class="form-select border-primary" asp-items="@(new SelectList(ViewBag.Units, "Name", "Name"))">
                                    <option value="">-- Chọn đơn vị --</option>
                                </select>
                                <span asp-validation-for="Unit" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold"><i class="fa fa-image me-1"></i> Ảnh chính (Tải lên)</label>
                                <input type="file" name="MainImage" class="form-control" accept="image/*" />
                                <div class="form-text">Nếu tải lên ảnh, hệ thống sẽ ưu tiên dùng ảnh này thay vì Link URL.</div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="ImageUrl" class="form-label fw-bold">Hoặc Link Ảnh (URL)</label>
                                <input asp-for="ImageUrl" class="form-control" placeholder="https://..." />
                                <span asp-validation-for="ImageUrl" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold"><i class="fa fa-images me-1"></i> Ảnh chi tiết / Gallery (Chọn nhiều ảnh)</label>
                            <input type="file" name="OtherImages" class="form-control" accept="image/*" multiple />
                            <div class="form-text">Bạn có thể chọn nhiều ảnh cùng lúc để làm bộ sưu tập ảnh cho sản phẩm.</div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Country" class="form-label fw-bold">Quốc gia / Xuất xứ</label>
                            <input asp-for="Country" class="form-control" placeholder="VD: Vietnam, Brazil..." />
                            <span asp-validation-for="Country" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label fw-bold">Mô tả</label>
                            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Manage" class="btn btn-secondary">Quay Lại</a>
                            <button type="submit" class="btn btn-success"><i class="fa fa-save"></i> Thêm Sản Phẩm</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>