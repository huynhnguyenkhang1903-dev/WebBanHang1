@model X.PagedList.IPagedList<Websitebanhang.Models.Product>
@using X.PagedList.Mvc.Core;

@{
    ViewData["Title"] = "Thực Đơn Cà Phê";
    var currentSort = Context.Request.Query["sortOrder"].ToString();
}

<div class="row fade-in">
    <!-- Breadcrumb -->
    <div class="col-12 mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a href="/" class="text-secondary"><i class="fa-solid fa-house"></i></a></li>
                <li class="breadcrumb-item active">Hạt Cà Phê</li>
            </ol>
        </nav>
        <h2 class="text-coffee mt-2 fw-bold">Khám Phá Cà Phê</h2>
    </div>

    <!-- SIDEBAR -->
    <div class="col-lg-3 mb-4">
        <div class="bg-white p-4 rounded-4 shadow-sm sticky-top" style="top:100px;">
            <h5 class="fw-bold mb-4 text-coffee"><i class="fa-solid fa-filter me-2"></i>Bộ Lọc</h5>

            <form method="get">
                <div class="mb-3 position-relative">
                    <input type="text" id="sidebarSearchInput" name="search" value="@Context.Request.Query["search"]" class="form-control" placeholder="Tên cà phê..." autocomplete="off">
                    <div id="sidebarSearchSuggestions" class="search-suggestions" style="width: 100%; top: 100%;"></div>
                </div>

                @{
                    var currentPrice = Context.Request.Query["price"].ToString();
                }

                <select name="price" class="form-select mb-3">
                    <option value="">Tất cả giá</option>
                    <option value="low" selected="@(currentPrice == "low")">Dưới 100k</option>
                    <option value="medium" selected="@(currentPrice == "medium")">100k - 300k</option>
                    <option value="high" selected="@(currentPrice == "high")">Trên 300k</option>
                </select>

                @{
                    var currentCountry = Context.Request.Query["country"].ToString();
                }

                <select name="country" class="form-select mb-3">
                    <option value="">Tất cả quốc gia</option>
                    <option value="Vietnam" selected="@(currentCountry == "Vietnam")">Việt Nam</option>
                    <option value="Brazil" selected="@(currentCountry == "Brazil")">Brazil</option>
                    <option value="Colombia" selected="@(currentCountry == "Colombia")">Colombia</option>
                </select>

                @{
                    var currentPromotion = Context.Request.Query["hasPromotion"].ToString();
                }

                <select name="hasPromotion" class="form-select mb-3">
                    <option value="">Tất cả sản phẩm</option>
                    <option value="true" selected="@(currentPromotion == "true")">🔥 Đang khuyến mãi</option>
                </select>

                <button class="btn btn-primary w-100">Lọc</button>
            </form>
        </div>
    </div>

    <!-- MAIN -->
    <div class="col-lg-9">

        <!-- HEADER + SORT -->
        <div class="d-flex justify-content-between align-items-center mb-4 bg-white p-3 rounded shadow-sm flex-wrap gap-2">

            <span>
                Hiển thị <b>@Model.Count</b> / @Model.TotalItemCount sản phẩm
            </span>

            <form method="get" class="d-flex gap-2">
                <!-- giữ filter -->
                <input type="hidden" name="search" value="@Context.Request.Query["search"]" />
                <input type="hidden" name="price" value="@Context.Request.Query["price"]" />
                <input type="hidden" name="country" value="@Context.Request.Query["country"]" />
                <input type="hidden" name="hasPromotion" value="@Context.Request.Query["hasPromotion"]" />

                <select name="sortOrder" class="form-select form-select-sm">
                    <option value="">-- Sắp xếp --</option>
                    <option value="name" selected="@(currentSort == "name")">Tên A-Z</option>
                    <option value="name_desc" selected="@(currentSort == "name_desc")">Tên Z-A</option>
                    <option value="price" selected="@(currentSort == "price")">Giá tăng</option>
                    <option value="price_desc" selected="@(currentSort == "price_desc")">Giá giảm</option>
                </select>

                <button class="btn btn-dark btn-sm">OK</button>
            </form>
        </div>

        <!-- PRODUCTS -->
        <div class="row g-4">
            @if (Model.Any())
            {
                foreach (var item in Model)
                {
                    <div class="col-6 col-md-4 col-xl-3">
                        <div class="card h-100 shadow-sm position-relative">
                            <a asp-controller="Wishlist" asp-action="Add" asp-route-id="@item.Id" asp-route-returnUrl="@Context.Request.Path"
                               class="position-absolute top-0 end-0 m-2 btn btn-light btn-sm rounded-circle shadow-sm z-index-3 text-danger" style="z-index: 3;" title="Thêm vào yêu thích">
                                <i class="fa-regular fa-heart"></i>
                            </a>
                            @if(item.Stock <= 0)
                            {
                                <span class="badge bg-secondary position-absolute top-0 start-0 m-2 z-index-2 shadow-sm rounded-pill">Hết hàng</span>
                            }
                            else if (item.Voucher != null && item.Voucher.ExpiryDate >= DateTime.Now)
                            {
                                <span class="badge bg-danger position-absolute top-0 start-0 m-2 z-index-2 shadow-sm rounded-pill">-@item.Voucher.DiscountPercent%</span>
                            }
                            <div class="position-relative overflow-hidden" style="aspect-ratio: 1/1;">
                                @if (!string.IsNullOrEmpty(item.ImageUrl))
                                {
                                    <img src="@item.ImageUrl" class="card-img-top w-100 h-100 object-fit-cover" onerror="this.src='https://placehold.co/400x400?text=Chua+co+anh'" />
                                }
                                else
                                {
                                    <div class="w-100 h-100 bg-light d-flex flex-column align-items-center justify-content-center text-muted">
                                        <i class="fa-regular fa-image fs-2 mb-2"></i>
                                        <small>Chưa có ảnh</small>
                                    </div>
                                }
                            </div>
                            <div class="card-body d-flex flex-column">
                                <a asp-controller="Product" asp-action="Display" asp-route-id="@item.Id" class="text-decoration-none text-dark">
                                    <h6>@item.Name</h6>
                                </a>
                                <div class="mt-auto d-flex justify-content-between align-items-center">
                                    <div>
                                        @if (item.Voucher != null && item.Voucher.ExpiryDate >= DateTime.Now)
                                        {
                                            <b class="text-danger">@((item.Price - item.Price * item.Voucher.DiscountPercent / 100).ToString("N0")) ₫</b>
                                            <br/>
                                            <span class="text-muted text-decoration-line-through small">@item.Price.ToString("N0") ₫</span>
                                        }
                                        else
                                        {
                                            <b class="text-danger">@item.Price.ToString("N0") ₫</b>
                                        }
                                    </div>
                                    @if(item.Stock > 0) {
                                        <form action="/Cart/AddToCart" method="post" class="m-0">
                                            <input type="hidden" name="id" value="@item.Id" />
                                            <button type="submit" class="btn btn-gold btn-sm"><i class="fa-solid fa-cart-plus"></i></button>
                                        </form>
                                    } else {
                                        <button class="btn btn-secondary btn-sm" disabled title="Hết hàng"><i class="fa-solid fa-ban"></i></button>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <p>Không có sản phẩm</p>
            }
        </div>

        <!-- PAGINATION -->
        @if (Model.PageCount > 1)
        {
            <div class="mt-4 d-flex justify-content-center">
                @Html.PagedListPager(Model, page => Url.Action("Index", new
                {
                    page,
                    search = Context.Request.Query["search"],
                    price = Context.Request.Query["price"],
                    country = Context.Request.Query["country"],
                    hasPromotion = Context.Request.Query["hasPromotion"],
                    sortOrder = Context.Request.Query["sortOrder"]
                }), new PagedListRenderOptions {
                    LiElementClasses = new string[] { "page-item" },
                    PageClasses = new string[] { "page-link" }
                })
            </div>
        }
    </div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const sidebarSearchInput = document.getElementById('sidebarSearchInput');
            const sidebarSuggestionsBox = document.getElementById('sidebarSearchSuggestions');
            let debounceTimer;

            if (sidebarSearchInput && sidebarSuggestionsBox) {
                sidebarSearchInput.addEventListener('input', function() {
                    clearTimeout(debounceTimer);
                    const term = this.value.trim();

                    if (term.length < 2) {
                        sidebarSuggestionsBox.style.display = 'none';
                        return;
                    }

                    debounceTimer = setTimeout(() => {
                        fetch(`/Product/SearchSuggestions?term=${encodeURIComponent(term)}`)
                            .then(response => response.json())
                            .then(data => {
                                if (data.length > 0) {
                                    sidebarSuggestionsBox.innerHTML = data.map(item => `
                                        <a href="/Product/Display/${item.id}" class="search-suggestion-item">
                                            <img src="${item.imageUrl || '/images/default-coffee.jpg'}" class="search-suggestion-img" alt="${item.name}">
                                            <div class="search-suggestion-info">
                                                <p class="search-suggestion-name">${item.name}</p>
                                                <span class="search-suggestion-price">${item.price.toLocaleString()} ₫</span>
                                            </div>
                                        </a>
                                    `).join('');
                                    sidebarSuggestionsBox.style.display = 'block';
                                } else {
                                    sidebarSuggestionsBox.style.display = 'none';
                                }
                            })
                            .catch(err => console.error('Sidebar search error:', err));
                    }, 300);
                });

                document.addEventListener('click', function(e) {
                    if (!sidebarSearchInput.contains(e.target) && !sidebarSuggestionsBox.contains(e.target)) {
                        sidebarSuggestionsBox.style.display = 'none';
                    }
                });
            }
        });
    </script>
}

</div>